# A module that I am goign to use as an example

def greet(what='Hello There', to='Mystry Person'):
    """
    A convenience function that returns a friendly string
    Parameters:
        what (str): The greeting to issue
        to (str): The person to greet
    Returns:
        a string that greets the person"""
    return f'{what} {to} (new version!)'